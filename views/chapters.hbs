<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Chapters</title>
    <style>
        * {
            font-size: 24px;
        }
    </style>
</head>
<body>
<ul class="chapters">
    {{#each chapters}}
        <li>
            {{#if this.isDirectory}}
                {{this.name}}
                <br />
                {{#each this.children}}
                    <ul class="sub-chapters">
                        <li>
                            <a href="/file?filepath={{this.link}}" target="_blank">{{this.name}}</a>
                        </li>
                    </ul>
                {{/each}}
            {{/if}}
        </li>
    {{/each}}
</ul>
</body>
</html>